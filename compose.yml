
services:
  bot:
    env_file:
      - .env
    image: steaminlinebot:latest
    build: .
    container_name: python-bot
    working_dir: /bot
    volumes:
      - ./data:/bot/data
    command: >
      sh -c "uv run bot 2>> /bot/data/errors.log"
    restart: unless-stopped
